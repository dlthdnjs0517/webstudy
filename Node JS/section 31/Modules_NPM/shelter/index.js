const blue = require('./blue')
const nunu = require('./nunu')
const yumi = require('./yumi')

const allCats = [blue, nunu, yumi]

module.exports = allCats;
//module.exports는 어떤 값을 외부로 내보낼지 정하는 객체.

//index 파일은 라이브러리로 작업할 때 중요한 역할을 수행한다.